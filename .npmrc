# @privy-io/react-auth pulls permissionless, which peer-wants ox@^0.8, while
# viem 2.56 ships ox@0.14. It is a peerOptional for ERC-4337 smart wallets,
# which PayGate does not use, so the mismatch is inert. Without this, a plain
# `npm install` fails with ERESOLVE.
legacy-peer-deps=true
